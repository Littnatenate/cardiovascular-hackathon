import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.medsafe.hackathon',
  appName: 'MedSafe',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
